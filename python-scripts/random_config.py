# Set seed for reproducibility and reuse in other files
import random
random.seed(2026)

# To reuse in other files, you can import this module and access the seed value as follows:
# from random_config import random
# random.seed(2026)